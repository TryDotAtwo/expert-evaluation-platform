{
  "name": "tupa-root-site",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && wrangler deploy --dry-run --config wrangler.jsonc",
    "deploy": "wrangler deploy --config wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "4.93.1"
  }
}
